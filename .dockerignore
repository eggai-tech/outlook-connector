.git
.github
.venv
__pycache__/
*.py[oc]
.pytest_cache
build/
dist/
wheels/
*.egg-info

# Secrets / local config — never bake into the image
.env*
config*.yaml
!config.yaml.example

.DS_Store
docs
tests
development

# Vendored library dev material — the wheel only needs libs/*/outlook_helper,
# its pyproject and its README.
libs/*/tests
libs/*/docs
libs/*/specs
libs/*/implementation
libs/*/AGENTS.md
libs/*/.python-version
